all:

    
  children:
    webservers:
      hosts:
        10.0.0.203:
           ansible_ssh_user: web
    dnservers:
      hosts:
        10.0.0.102:
           ansible_ssh_user: dns
    dhcp-server:
      hosts:
        10.0.0.254:
           ansible_ssh_user: dabo
